services:
  # Postgres 설정
  # - https://hub.docker.com/_/postgres
  grid-app-db:
    image: postgres:14.5-alpine
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: grid-app
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
